{
  "name": "documentcheck-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "项目根 — 仅承载 Playwright L3 UI E2E 与 seed 脚本,backend/frontend 各自独立",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:install": "playwright install --with-deps",
    "seed": "tsx e2e/seed.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "~5.6.0"
  }
}
